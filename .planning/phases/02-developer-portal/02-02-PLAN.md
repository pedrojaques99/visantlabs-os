---
phase: 02-developer-portal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/GettingStartedPage.tsx
  - src/App.tsx
autonomous: true
requirements: [PORTAL-03]
must_haves:
  truths:
    - "Developer can follow a getting-started guide with copy-paste code examples"
    - "Developer sees examples for at least three tool categories (brand generation, mockups, creative)"
    - "Developer can copy code snippets with one click"
  artifacts:
    - path: "src/pages/GettingStartedPage.tsx"
      provides: "Getting started guide with code examples"
      min_lines: 150
  key_links:
    - from: "src/pages/GettingStartedPage.tsx"
      to: "/api/docs"
      via: "link to full API docs"
      pattern: "api/docs|swagger"
---

<objective>
Create a getting-started guide page with copy-paste code examples for the Visant API, covering authentication and at least three tool categories.

Purpose: PORTAL-03 requires developers to self-onboard with code examples per tool category.
Output: New `/developer/getting-started` page with structured guide and copyable code snippets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/pages/ApiKeysPage.tsx
@server/routes/apiKeys.ts
@server/middleware/apiKeyAuth.ts
</context>

<interfaces>
<!-- API key format and auth pattern -->
```typescript
// Auth: Bearer visant_sk_xxxx
// Scopes: read, write, generate
// Endpoint: POST /api/mcp (MCP Streamable HTTP)
// Request body follows MCP protocol: { method: "tools/call", params: { name: "tool-name", arguments: {...} } }
```

<!-- Existing page patterns -->
```typescript
import { Card, CardContent } from '../components/ui/card';
import { useLayout } from '@/hooks/useLayout';
import { BreadcrumbWithBack, ... } from "../components/ui/BreadcrumbWithBack";
import { SEO } from '../components/SEO';
import { GridDotsBackground } from '../components/ui/GridDotsBackground';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GettingStartedPage with code examples</name>
  <files>src/pages/GettingStartedPage.tsx, src/App.tsx</files>
  <read_first>src/pages/ApiKeysPage.tsx (layout patterns), src/pages/DocsPage.tsx (if it has doc patterns), server/middleware/apiKeyAuth.ts (auth flow)</read_first>
  <action>
Create `src/pages/GettingStartedPage.tsx` — a developer getting-started guide page.

**Layout:** Same patterns as ApiKeysPage (BreadcrumbWithBack, useLayout, GridDotsBackground, SEO).

**Structure — sections with anchors:**

1. **Authentication** — How to get an API key (link to `/settings/api-keys`), how to use it:
```bash
curl -X POST https://api.visant.dev/api/mcp \
  -H "Authorization: Bearer visant_sk_YOUR_KEY" \
  -H "Content-Type: application/json" \
  -d '{"method": "tools/call", "params": {"name": "list-tools", "arguments": {}}}'
```

2. **Brand Generation** (tool category 1) — Example calling a brand generation tool:
```javascript
const response = await fetch('https://api.visant.dev/api/mcp', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer visant_sk_YOUR_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    method: 'tools/call',
    params: { name: 'generate-brand-guidelines', arguments: { brandName: 'Acme Corp', industry: 'Technology' } }
  })
});
```

3. **Mockup Generation** (tool category 2) — Example calling mockup tools with similar pattern.

4. **Creative Studio** (tool category 3) — Example calling creative/canvas tools.

5. **Next Steps** — Links to full API docs (`/api/docs`), API keys page, usage dashboard.

**Code block component:** Create a simple inline `CodeBlock` component (NOT a design system component — just a local helper inside this file) with:
- `<pre><code>` with monospace font and dark background
- Copy button (top-right) using navigator.clipboard.writeText
- Language label (bash, javascript, python)
- Show examples in both JavaScript and Python (tab toggle)

**Sidebar nav:** Simple sticky list of section anchors for quick navigation.

**Route:** Add to App.tsx: `<Route path="/developer/getting-started" element={<GettingStartedPage />} />` near the usage route.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- Page renders at /developer/getting-started
- Contains authentication section with curl example
- Contains at least 3 tool category sections with JS and Python code examples
- Copy button copies code to clipboard
- Links to /api/docs, /settings/api-keys
- Uses existing design system components for layout
  </acceptance_criteria>
  <done>Developer can follow a step-by-step getting-started guide with copyable code examples for authentication, brand generation, mockups, and creative tools</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. /developer/getting-started renders with all sections
3. Copy buttons work
4. Code examples show correct API endpoint and auth header format
</verification>

<success_criteria>
Developer can visit /developer/getting-started and follow a structured guide with copy-paste code examples covering auth + 3 tool categories.
</success_criteria>

<output>
After completion, create `.planning/phases/02-developer-portal/02-02-SUMMARY.md`
</output>
