---
phase: 02-developer-portal
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/pages/DeveloperPortalPage.tsx
  - src/pages/ApiKeysPage.tsx
  - src/App.tsx
autonomous: false
requirements: [PORTAL-01]
must_haves:
  truths:
    - "Developer can access a portal hub linking to keys, usage, docs, and getting started"
    - "Developer can create a new API key with selected scopes from the dashboard"
    - "Developer can revoke an API key and it is immediately rejected"
    - "API keys page shows clear status (active/revoked/expired) for each key"
  artifacts:
    - path: "src/pages/DeveloperPortalPage.tsx"
      provides: "Developer portal hub page"
      min_lines: 60
    - path: "src/pages/ApiKeysPage.tsx"
      provides: "Polished API key management page"
  key_links:
    - from: "src/pages/DeveloperPortalPage.tsx"
      to: "/settings/api-keys"
      via: "Link component"
      pattern: "settings/api-keys"
    - from: "src/pages/DeveloperPortalPage.tsx"
      to: "/developer/usage"
      via: "Link component"
      pattern: "developer/usage"
    - from: "src/pages/DeveloperPortalPage.tsx"
      to: "/developer/getting-started"
      via: "Link component"
      pattern: "developer/getting-started"
---

<objective>
Create the developer portal hub page and polish the existing API keys page to complete the self-serve key management flow.

Purpose: PORTAL-01 requires developers to create, revoke, and manage API keys via dashboard. The hub page ties all portal features together.
Output: New `/developer` hub page + polished ApiKeysPage with clear UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/pages/ApiKeysPage.tsx
@.planning/phases/02-developer-portal/02-01-SUMMARY.md
@.planning/phases/02-developer-portal/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeveloperPortalPage hub and polish ApiKeysPage</name>
  <files>src/pages/DeveloperPortalPage.tsx, src/pages/ApiKeysPage.tsx, src/App.tsx</files>
  <read_first>src/pages/ApiKeysPage.tsx (full file), src/App.tsx (route section), src/pages/UsageDashboardPage.tsx (first 20 lines for import pattern)</read_first>
  <action>
**DeveloperPortalPage.tsx** — Hub page at `/developer` with 4 cards linking to:
1. API Keys (`/settings/api-keys`) — "Manage your API keys" with Key icon
2. Usage Analytics (`/developer/usage`) — "Monitor API consumption" with BarChart icon
3. Getting Started (`/developer/getting-started`) — "Code examples and guides" with BookOpen icon
4. API Reference (`/api/docs`) — "Interactive API documentation" with FileText icon (external link, opens in new tab)

Use Card/CardContent components, Link from react-router-dom, lucide-react icons. Follow same layout pattern as other pages (BreadcrumbWithBack, useLayout, GridDotsBackground, SEO).

**ApiKeysPage.tsx polish:**
- Add a "Back to Developer Portal" link at top (Link to `/developer`)
- Update breadcrumb to include Developer Portal as parent
- Ensure the "create key" flow clearly shows the raw key only once with prominent copy button and warning
- Ensure revoke confirmation is clear (already has dialog — verify it works)
- No functional changes needed to the create/list/revoke logic — it already works

**Routes in App.tsx:**
- Add `<Route path="/developer" element={<DeveloperPortalPage />} />`
- Keep existing `/settings/api-keys` route unchanged
- Add import for DeveloperPortalPage
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- /developer renders hub page with 4 navigation cards
- All 4 links work (keys, usage, getting-started, api docs)
- ApiKeysPage has breadcrumb linking back to /developer
- Create/revoke key flows work end-to-end
  </acceptance_criteria>
  <done>Developer portal hub exists at /developer with navigation to all portal features, and API keys page is properly integrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete developer portal: hub page, API key management, usage dashboard with charts, and getting-started guide with code examples</what-built>
  <how-to-verify>
    1. Visit /developer — see 4 navigation cards (API Keys, Usage, Getting Started, API Reference)
    2. Click "API Keys" — create a new key with read+generate scopes, copy the key, verify it appears in the list
    3. Revoke the key, confirm status changes to "revoked"
    4. Go back to /developer, click "Usage Analytics" — see stats cards and historical chart
    5. Click "Getting Started" — see auth section, 3 tool category examples, copy a code snippet
    6. Click "API Reference" — opens /api/docs (Swagger UI) in new tab
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. All TypeScript compiles without errors
2. All 4 developer portal pages render correctly
3. API key create/revoke flow works end-to-end
4. Usage dashboard shows real data
5. Getting-started code examples are copyable and accurate
</verification>

<success_criteria>
Complete developer portal accessible at /developer with working key management, usage analytics, getting-started guide, and link to API docs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-developer-portal/02-03-SUMMARY.md`
</output>
