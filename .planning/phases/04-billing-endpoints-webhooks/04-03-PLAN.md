---
phase: 04-billing-endpoints-webhooks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PricingPage.tsx
  - src/App.tsx
autonomous: false
requirements: [BILL-02]

must_haves:
  truths:
    - "Public pricing page lists API tiers with per-call credit costs"
    - "Page shows rate limits per tier"
    - "Page is accessible without authentication"
  artifacts:
    - path: "src/pages/PricingPage.tsx"
      provides: "Public pricing page component"
      exports: ["default"]
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/PricingPage.tsx"
      via: "React Router route"
      pattern: "/pricing"
---

<objective>
Create a public API pricing page showing tiers, per-call costs, and rate limits.

Purpose: BILL-02 — developers can understand pricing before integrating.
Output: React page at /pricing using existing design system components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/utils/pricing.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PricingPage component with tier cards and rate card</name>
  <files>src/pages/PricingPage.tsx</files>
  <action>
Create src/pages/PricingPage.tsx using existing design system components (check src/components/ for Card, Button, etc. — use what exists, do NOT create new components).

Content structure:
1. **Header section**: "API Pricing" title, subtitle "Pay-as-you-go credits. No minimum commitment."

2. **Tier cards** (3 columns on desktop, stack on mobile):
   - **Free**: 20 credits/month, 100 req/15min, basic scopes (read)
   - **Creator**: 200 credits/month, 500 req/15min, all scopes
   - **Studio**: 1000 credits/month, 2000 req/15min, all scopes + priority

3. **Per-call credit costs table**:
   | Operation | Credits |
   |-----------|---------|
   | Image generation (512px) | 1 |
   | Image generation (1K/HD) | 2 |
   | Image generation (2K) | 3 |
   | Image generation (4K) | 4-7 |
   | Video generation (fast) | 15 |
   | Video generation (standard) | 40 |
   | Text/analysis call | 0 (token-based) |

   These values come from server/utils/usageTracking.ts getCreditsRequired() — hardcode them as static data in the page (no API call needed for a public pricing page).

4. **Rate limits section**: Table showing tier vs requests/15min

5. **CTA**: "Get Started" button linking to /developer (the developer portal)

Use Tailwind classes consistent with the app. Use the app's existing layout wrapper if one exists. Page must be publicly accessible (no auth guard).
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>PricingPage.tsx renders tier cards, credit cost table, and rate limits</done>
</task>

<task type="auto">
  <name>Task 2: Add /pricing route to App.tsx</name>
  <files>src/App.tsx</files>
  <action>
In src/App.tsx:
- Import PricingPage: `const PricingPage = lazy(() => import('./pages/PricingPage'))`
- Add public route (no auth wrapper): `<Route path="/pricing" element={<PricingPage />} />`
- Place it alongside other public routes (look for patterns like /login, /waitlist that have no auth guard)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>/pricing route registered, accessible without authentication</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Public API pricing page at /pricing with tier cards, credit costs, and rate limits</what-built>
  <how-to-verify>
    1. Run dev server: `npm run dev`
    2. Visit http://localhost:5173/pricing (or whatever port)
    3. Verify: 3 tier cards visible (Free, Creator, Studio)
    4. Verify: Credit cost table shows per-operation costs
    5. Verify: Rate limits displayed per tier
    6. Verify: Page loads without authentication
    7. Verify: Design is consistent with rest of app
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep "pricing" src/App.tsx` shows route registered
- Page renders at /pricing without auth
</verification>

<success_criteria>
Public pricing page is live at /pricing showing API tiers, per-call credit costs, and rate limits. Design uses existing components.
</success_criteria>

<output>
After completion, create `.planning/phases/04-billing-endpoints-webhooks/04-03-SUMMARY.md`
</output>
