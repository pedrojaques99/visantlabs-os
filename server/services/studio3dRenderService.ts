/**
 * Server-side 3D export service — SVG → GLB.
 *
 * Visual rendering (PNG/GIF/MP4) happens client-side via auto-render deep links.
 * The 3D Studio detects ?autoRender=gif|mp4|webm|png in the URL and triggers
 * the turntable export automatically using the user's GPU.
 *
 * This file is kept as a placeholder for future server-side rendering
 * if WebGL2 headless support becomes available (e.g. via headless Chrome).
 */

export {};
