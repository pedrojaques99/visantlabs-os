/**
 * Server-side Halftone SVG Renderer
 *
 * Thin re-export: the CMYK halftone SVG generator now lives in @visant/print-fx
 * (`./halftone`). Kept here so existing server importers keep their path.
 */
export { generateHalftoneSvg } from '@visant/print-fx/halftone';
