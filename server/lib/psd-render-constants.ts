// Re-export fino de @visant/psd-engine — single source of truth.
// Mantido por compat de import path; novos consumidores importam do pacote.
export { SO_TARGET, BRAND_HIDE, SO_DECOR } from '@visant/psd-engine';
